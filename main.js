// Write your code below
// Write your code below
function printCoordinates(size) {
    for (var x = 0; x < size; x++) {
        for (var y = 0; y < size; y++) {
            console.log("(" + x + ", " + y + ")");
        }
    }
}
